### Quick run steps (Firefox / Zen)

- Load the folder as a temporary add-on (Firefox: about:debugging → “This Firefox” → “Load Temporary Add-on…” → select manifest.json).

- Open the extension’s Options page and save your Name/Email/Address.

- Visit any site with forms; it will autofill on page load and when SPAs add fields dynamically.
